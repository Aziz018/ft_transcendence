import Fuego from "../../index";
import { Link } from "../../library/Router/Router";

const Button = () => {
  return (
    <button
      type="button"
      class="text-[14px] text-light border-solid border-light border-[1px] rounded-[30px] px-16 cursor-pointer py-2">
      Get started
    </button>
  );
};

export default Button;
